# Evidence policy

Commit only reviewed, non-secret metadata. Never store seeds, private keys, passphrases, tokens, raw preimages/witnesses, `.env`, or private transcripts. `offline-rehearsal.json` is generated through a redacting serializer and contains safe observations only.
